export default defineAppConfig({
    ui: {
        colors: {
            primary: 'green',
            // neutral: 'slate'
        },
        container: {
            base: 'w-full max-w-(--ui-container) mx-auto px-4 py-4 sm:px-6 lg:px-8',
        },
    },
});
